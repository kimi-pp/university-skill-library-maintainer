{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openecon.ai/econ-review/review-actions.schema.json",
  "title": "econ-review author action handoff",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "kind", "source_review_id", "source_review_fingerprint",
    "source_manuscripts", "exported_at", "entries"
  ],
  "properties": {
    "schema_version": {"enum": ["0.1", "0.2", "0.3", "0.4"]},
    "kind": {"const": "econ-review-actions"},
    "source_review_id": {"type": "string", "minLength": 1, "pattern": "^\\S(?:[\\s\\S]*\\S)?$"},
    "source_review_fingerprint": {
      "type": "string",
      "minLength": 1,
      "pattern": "^\\S(?:[\\s\\S]*\\S)?$",
      "description": "For v0.4 exports, the lowercase SHA-256 of the exact BOM-free UTF-8 findings.json bytes loaded by Review Desk; older action contracts retain their declared legacy fingerprint."
    },
    "source_manuscripts": {
      "type": "array",
      "maxItems": 200,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256"],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(?!/)(?![A-Za-z]:)(?!.*\\\\)(?!.*(?:^|/)\\.\\.(?:/|$))\\S(?:[^\\r\\n]*\\S)?$",
            "description": "Portable manuscript label only (normally a basename, disambiguated when needed); never an absolute local path."
          },
          "sha256": {"type": ["string", "null"], "pattern": "^[a-f0-9]{64}$"}
        }
      }
    },
    "exported_at": {"type": "string", "format": "date-time"},
    "entries": {
      "type": "array",
      "maxItems": 20000,
      "items": {"$ref": "#/$defs/entry"}
    }
  },
  "allOf": [
    {
      "if": {"properties": {"schema_version": {"const": "0.1"}}, "required": ["schema_version"]},
      "then": {
        "properties": {
          "entries": {
            "items": {
              "allOf": [
                {
                  "if": {"properties": {"disposition": {"const": "ready_for_recheck"}}, "required": ["disposition"]},
                  "then": {
                    "anyOf": [
                      {"properties": {"response_note": {"type": "string", "pattern": "\\S"}}, "required": ["response_note"]},
                      {"properties": {"changed_locations": {"type": "array", "minItems": 1}}, "required": ["changed_locations"]}
                    ]
                  }
                },
                {
                  "if": {"properties": {"disposition": {"const": "challenged"}}, "required": ["disposition"]},
                  "then": {"properties": {"response_note": {"type": "string", "pattern": "\\S"}}, "required": ["response_note"]}
                }
              ]
            }
          }
        }
      }
    },
    {
      "if": {"properties": {"schema_version": {"const": "0.3"}}, "required": ["schema_version"]},
      "then": {"properties": {"entries": {"items": {"required": ["events"]}}}}
    },
    {
      "if": {"properties": {"schema_version": {"const": "0.4"}}, "required": ["schema_version"]},
      "then": {
        "properties": {
          "entries": {
            "items": {
              "required": ["events", "user_priority", "reviewed"]
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {"schema_version": {"enum": ["0.1", "0.2", "0.3"]}},
        "required": ["schema_version"]
      },
      "then": {
        "properties": {
          "entries": {
            "items": {
              "properties": {
                "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred"]},
                "status_history": {
                  "items": {
                    "properties": {
                      "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred"]}
                    }
                  }
                },
                "events": {
                  "items": {
                    "properties": {
                      "type": {"enum": ["disposition_changed", "note_revised", "imported", "reversed"]},
                      "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred"]}
                    }
                  }
                }
              },
              "not": {
                "anyOf": [
                  {"required": ["user_priority"]},
                  {"required": ["reviewed"]}
                ]
              }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "history_entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["disposition", "at"],
      "properties": {
        "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred", "not_relevant", "not_addressable"]},
        "at": {"type": "string", "format": "date-time"}
      }
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "finding_id", "disposition", "response_note", "changed_locations",
        "updated_at", "status_history"
      ],
      "properties": {
        "finding_id": {"type": "string", "pattern": "^[A-Z][A-Z0-9_-]*-[0-9]{2,}$"},
        "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred", "not_relevant", "not_addressable"]},
        "response_note": {"type": "string", "maxLength": 10000},
        "changed_locations": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1, "maxLength": 1000, "pattern": "^\\S(?:[\\s\\S]*\\S)?$"}
        },
        "updated_at": {"type": "string", "format": "date-time"},
        "status_history": {
          "type": "array",
          "minItems": 1,
          "maxItems": 10000,
          "items": {"$ref": "#/$defs/history_entry"}
        },
        "events": {
          "type": "array",
          "minItems": 1,
          "maxItems": 10000,
          "items": {"$ref": "#/$defs/event"}
        },
        "user_priority": {"enum": ["P0", "P1", "P2", null]},
        "reviewed": {"type": "boolean"}
      },
      "allOf": [
        {
          "if": {
            "properties": {"disposition": {"enum": ["not_relevant", "not_addressable"]}},
            "required": ["disposition"]
          },
          "then": {"properties": {"reviewed": {"const": true}}, "required": ["reviewed"]}
        }
      ],
      "description": "Author disposition, personal priority, reviewed state, and optional note. Canonical severity is unchanged. A later review independently verifies every ready-for-recheck or challenged state."
    },
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "type", "at", "parent_event_id", "origin"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "type": {"enum": ["disposition_changed", "note_revised", "priority_changed", "reviewed_changed", "imported", "reversed"]},
        "at": {"type": "string", "format": "date-time"},
        "disposition": {"enum": ["open", "ready_for_recheck", "challenged", "deferred", "not_relevant", "not_addressable"]},
        "note": {"type": ["string", "null"], "maxLength": 10000},
        "user_priority": {"enum": ["P0", "P1", "P2", null]},
        "reviewed": {"type": "boolean"},
        "parent_event_id": {"type": ["string", "null"], "format": "uuid"},
        "origin": {"enum": ["local", "import"]}
      },
      "allOf": [
        {
          "if": {"properties": {"type": {"const": "disposition_changed"}}, "required": ["type"]},
          "then": {
            "required": ["disposition"],
            "not": {"anyOf": [{"required": ["note"]}, {"required": ["user_priority"]}, {"required": ["reviewed"]}]}
          }
        },
        {
          "if": {"properties": {"type": {"const": "note_revised"}}, "required": ["type"]},
          "then": {
            "required": ["note"],
            "not": {"anyOf": [{"required": ["disposition"]}, {"required": ["user_priority"]}, {"required": ["reviewed"]}]}
          }
        },
        {
          "if": {"properties": {"type": {"const": "priority_changed"}}, "required": ["type"]},
          "then": {
            "required": ["user_priority"],
            "not": {"anyOf": [{"required": ["disposition"]}, {"required": ["note"]}, {"required": ["reviewed"]}]}
          }
        },
        {
          "if": {"properties": {"type": {"const": "reviewed_changed"}}, "required": ["type"]},
          "then": {
            "required": ["reviewed"],
            "not": {"anyOf": [{"required": ["disposition"]}, {"required": ["note"]}, {"required": ["user_priority"]}]}
          }
        },
        {
          "if": {"properties": {"type": {"const": "imported"}}, "required": ["type"]},
          "then": {
            "properties": {"origin": {"const": "import"}},
            "not": {"anyOf": [{"required": ["disposition"]}, {"required": ["note"]}, {"required": ["user_priority"]}, {"required": ["reviewed"]}]}
          }
        },
        {
          "if": {"properties": {"type": {"const": "reversed"}}, "required": ["type"]},
          "then": {
            "properties": {"parent_event_id": {"type": "string", "format": "uuid"}},
            "anyOf": [{"required": ["disposition"]}, {"required": ["note"]}],
            "not": {"anyOf": [{"required": ["user_priority"]}, {"required": ["reviewed"]}]}
          }
        }
      ]
    }
  }
}
