使用 `$university-skill-library-maintainer` 执行本项目已应用设置对应的自动维护。

- 绝对项目根：`{{ABSOLUTE_PROJECT_ROOT}}`
- 绝对 TOML 路径：`{{ABSOLUTE_TOML_PATH}}`
- 已应用 TOML SHA-256：`{{APPLIED_TOML_SHA256}}`
- 固定命令：`scheduled-run`

只在上述项目根内按 Skill 契约执行。重新读取 TOML 并核对 SHA-256；不一致、禁用或手动模式时停止。保持同一长驻进程完成材料事实观察、项目评审决定与逐页视觉三闸，不得跨进程恢复 capability。不得覆盖目标或专业范围，不得添加平台或范围参数。无变化且成功时静默结束；仅在发现变化、需要人工决定或失败时通知。
